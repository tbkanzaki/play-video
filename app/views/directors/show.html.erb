<div class="container shadow p-2 mb-4 bg-body-tertiary rounded">
  <h2 class="text-center">Filmes do diretor <%= @director.name %></h2>
</div>
<div class="container text-center text-success"><%= notice %></div>
<div class="container shadow p-2 mb-4 bg-body-tertiary rounded">
  <% if @movies.any? %>
    <div id="movies">
      <dl class="row p-3 text-muted">
        <% @movies.each do |movie| %>
          <dt class="col-sm-12">
            <%= movie.title %> - <%= movie.release_year %>
          </dt>
        <% end %>
      </dl>
    </div>
  <% end %>  
  <div class="mt-4">
  <%= link_to "Back to genres", directors_path %>
</div>
</div>